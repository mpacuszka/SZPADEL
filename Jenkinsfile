pipeline {
    agent any

    stages {
        stage("pytest") {
            steps {
                echo "pytest echo"
            }
        }

        stage("pytest coverage") {
            steps {
                echo "coverage echo"
            }
        }
    }
}